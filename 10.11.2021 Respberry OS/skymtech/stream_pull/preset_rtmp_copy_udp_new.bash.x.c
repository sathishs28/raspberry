#if 0
	shc Version 4.0.3, Generic Shell Script Compiler
	GNU GPL Version 3 Md Jahidul Hamid <jahidulhamid@yahoo.com>

	shc -U -f preset_rtmp_copy_udp_new.bash -o preset_rtmp_copy_udp_ori 
#endif

static  char data [] = 
#define      pswd_z	256
#define      pswd	((&data[0]))
	"\352\233\013\107\233\033\156\366\277\364\200\050\345\123\062\201"
	"\264\141\230\127\366\120\074\071\161\041\113\226\100\272\037\053"
	"\126\052\162\361\106\341\350\005\326\150\056\274\273\140\075\160"
	"\302\326\310\271\047\004\363\231\046\076\057\146\370\116\222\117"
	"\171\005\100\300\346\051\305\275\221\364\171\115\124\267\275\027"
	"\216\205\320\265\212\303\116\260\001\176\027\372\315\251\111\107"
	"\256\212\007\225\263\315\122\105\301\314\222\026\204\120\055\022"
	"\325\375\307\137\301\026\020\303\224\047\276\141\320\010\250\176"
	"\223\257\023\107\174\146\214\076\062\036\124\267\156\201\311\104"
	"\177\220\244\101\246\264\004\073\333\303\234\254\313\105\052\136"
	"\365\076\245\162\245\062\260\330\121\004\217\300\205\130\004\005"
	"\350\250\106\217\135\113\312\071\017\147\345\332\254\020\071\241"
	"\116\337\023\363\022\303\313\143\310\132\043\116\262\047\123\233"
	"\320\232\052\056\345\364\147\365\134\114\317\010\134\011\252\253"
	"\351\276\236\373\202\152\136\112\305\201\230\167\251\354\023\172"
	"\206\075\250\154\062\020\141\216\134\061\227\271\072\101\144\044"
	"\202\156\176\314\063\000\145\253"
#define      msg2_z	19
#define      msg2	((&data[265]))
	"\044\274\005\174\056\202\011\105\027\220\250\250\314\373\204\307"
	"\213\360\347\117\327\374\315"
#define      inlo_z	3
#define      inlo	((&data[287]))
	"\031\101\142"
#define      rlax_z	1
#define      rlax	((&data[290]))
	"\273"
#define      text_z	764
#define      text	((&data[355]))
	"\022\227\130\066\227\133\125\031\311\324\346\375\324\113\250\176"
	"\235\147\242\165\144\157\271\223\114\137\121\206\066\246\172\111"
	"\076\322\177\325\055\325\357\367\251\325\365\175\041\236\373\276"
	"\005\236\063\151\015\355\375\132\115\116\341\203\365\133\314\063"
	"\065\325\176\006\022\265\353\174\061\177\355\152\347\335\236\130"
	"\350\072\130\115\204\226\213\045\063\335\366\225\266\031\374\174"
	"\376\034\363\226\062\255\061\267\026\221\246\246\155\251\232\325"
	"\130\055\243\017\173\160\363\105\102\202\044\355\153\251\202\156"
	"\132\006\067\302\225\057\266\256\371\105\005\226\143\207\270\317"
	"\043\355\342\057\272\132\327\305\236\164\337\071\051\050\175\025"
	"\035\132\256\106\242\252\113\236\175\163\344\004\201\143\020\250"
	"\335\054\250\212\243\023\021\314\373\054\004\317\235\313\344\060"
	"\253\054\032\036\253\270\120\113\245\247\377\213\066\021\353\255"
	"\231\150\217\324\170\266\232\201\000\302\225\313\064\122\203\105"
	"\035\171\116\320\263\064\274\316\326\315\063\041\021\301\027\200"
	"\075\240\024\003\260\032\302\243\364\273\374\174\126\142\357\215"
	"\220\066\156\327\314\166\245\046\146\232\250\203\217\365\016\231"
	"\244\277\257\011\106\004\366\003\312\224\320\061\030\372\302\141"
	"\261\347\351\371\146\351\017\030\111\304\363\072\135\135\030\147"
	"\114\227\151\060\002\213\230\033\214\150\216\306\347\330\330\257"
	"\010\157\133\315\101\161\010\171\071\277\367\177\020\173\253\257"
	"\065\161\023\142\170\302\252\014\115\230\032\231\237\273\071\267"
	"\341\276\164\133\335\121\320\370\335\236\077\233\347\156\250\026"
	"\264\165\130\302\206\123\156\124\222\132\373\126\035\001\351\153"
	"\110\165\314\265\160\122\231\076\053\141\040\373\236\077\006\206"
	"\004\071\100\137\212\066\041\352\052\200\067\171\357\036\040\173"
	"\226\201\231\006\210\076\060\336\127\225\161\123\145\114\127\205"
	"\260\311\317\344\063\033\117\233\235\215\313\136\060\262\001\172"
	"\150\150\070\157\045\177\235\146\157\062\105\056\347\042\073\334"
	"\221\231\061\357\024\325\356\215\121\325\332\066\130\010\060\236"
	"\130\242\216\320\054\215\162\006\206\036\206\254\145\224\342\350"
	"\325\254\122\113\132\172\224\175\301\255\075\224\277\172\126\026"
	"\031\277\141\141\034\206\353\306\306\064\362\043\125\220\247\257"
	"\133\076\121\373\253\121\311\215\126\316\076\244\131\276\001\210"
	"\300\112\241\114\160\103\264\022\301\277\072\342\141\303\134\111"
	"\251\007\006\256\306\157\267\066\053\020\016\322\214\105\232\033"
	"\302\371\056\241\202\256\202\042\263\167\164\274\041\232\233\345"
	"\052\115\147\103\306\262\203\344\327\215\343\045\371\241\023\057"
	"\053\100\037\015\345\024\122\070\052\104\302\170\167\350\376\155"
	"\167\104\211\237\012\054\317\361\300\155\371\016\214\315\166\136"
	"\236\140\234\133\007\273\347\317\020\065\232\302\332\223\052\175"
	"\124\357\136\124\271\072\241\242\260\136\227\324\305\374\131\261"
	"\127\062\047\066\317\265\230\133\355\375\356\366\375\266\046\216"
	"\233\103\041\236\341\066\317\045\351\275\350\345\117\322\365\272"
	"\261\160\346\245\027\307\225\055\300\074\271\122\124\335\120\223"
	"\010\176\212\305\074\166\026\263\062\220\262\364\012\235\373\350"
	"\173\223\124\206\256\065\304\010\063\365\355\066\020\023\235\145"
	"\311\041\112\154\101\101\065\165\030\045\312\236\236\075\266\102"
	"\172\065\156\023\042\057\126\174\260\067\242\160\077\346\021\375"
	"\143\270\102\104\143\370\177\230\271\126\210\321\346\033\203\102"
	"\140\344\046\240\353\220\216\077\127\161\217\072\377\257\032\326"
	"\306\042\223\252\330\377\330\305\310\101\013\135\056\114\011\134"
	"\041\370\123\313\316\111\111\357\347\104\256\354\343\342\126\360"
	"\320\123\113\035\241\055\240\227\211\155\312\267\272\324\023\333"
	"\314\147\247\233\260\360\213\227\065\071\204\030\034\332\011\354"
	"\056\124\011\320\202\252\147\013\030\062\303\322\006\326\256\323"
	"\076\125\156\356\105\371\205\172\063\012\223\117\345\234\073\023"
	"\361\105\344\163\357\114\177\007\176\102\332\205\031\210\130\127"
	"\336\307\105\043\300\313\236\364\326\061\103\273\316\177\317\277"
	"\304\263\063\264\377\262\273\176\365\226\004\016\036\135\146\374"
	"\044\253\040\345\167\277\331\115\361\034\011\277\234\330\177\140"
	"\214\262\025\214\144\321\012\132\147\017\150\205\154\316\202\220"
	"\172\243\165\362\143\116\077\125\153\111\024\007\041\224\150\255"
	"\106\175"
#define      shll_z	10
#define      shll	((&data[1303]))
	"\104\005\152\313\236\343\373\110\163\314\254\313"
#define      opts_z	1
#define      opts	((&data[1313]))
	"\045"
#define      xecc_z	15
#define      xecc	((&data[1317]))
	"\276\120\267\247\265\346\104\221\054\147\153\367\201\114\016\061"
	"\257\262"
#define      lsto_z	1
#define      lsto	((&data[1332]))
	"\234"
#define      tst2_z	19
#define      tst2	((&data[1333]))
	"\377\365\021\206\263\321\261\217\021\006\156\111\047\173\154\030"
	"\125\205\023\032\200\062\057"
#define      tst1_z	22
#define      tst1	((&data[1357]))
	"\360\146\342\106\312\101\126\075\140\070\001\355\117\321\307\121"
	"\254\131\055\207\302\272\214\001\012\156\073"
#define      chk2_z	19
#define      chk2	((&data[1386]))
	"\273\163\324\252\162\056\256\137\161\251\222\352\273\365\044\044"
	"\370\015\042\074\372\263\052\257"
#define      chk1_z	22
#define      chk1	((&data[1409]))
	"\344\036\116\061\102\362\067\057\043\074\321\063\370\133\277\134"
	"\145\110\070\154\316\325\060\250\320\253"
#define      msg1_z	65
#define      msg1	((&data[1442]))
	"\261\260\100\344\340\310\070\244\271\230\220\205\230\124\340\331"
	"\011\150\325\114\072\234\054\040\321\025\301\157\300\216\244\364"
	"\266\075\035\056\300\067\164\173\247\112\141\161\103\351\326\276"
	"\166\166\232\337\137\230\145\142\271\046\250\375\337\334\242\070"
	"\277\207\335\275\260\255\204\150\101\276\072\256\050\165\145\345"
	"\366\040\131\312\113\010"
#define      date_z	1
#define      date	((&data[1519]))
	"\343"/* End of data[] */;
#define      hide_z	4096
#define SETUID 0	/* Define as 1 to call setuid(0) at start of script */
#define DEBUGEXEC	0	/* Define as 1 to debug execvp calls */
#define TRACEABLE	0	/* Define as 1 to enable ptrace the executable */
#define HARDENING	0	/* Define as 1 to disable ptrace/dump the executable */
#define BUSYBOXON	0	/* Define as 1 to enable work with busybox */

#if HARDENING
static const char * shc_x[] = {
"/*",
" * Copyright 2019 - Intika <intika@librefox.org>",
" * Replace ******** with secret read from fd 21",
" * Also change arguments location of sub commands (sh script commands)",
" * gcc -Wall -fpic -shared -o shc_secret.so shc_secret.c -ldl",
" */",
"",
"#define _GNU_SOURCE /* needed to get RTLD_NEXT defined in dlfcn.h */",
"#define PLACEHOLDER \"********\"",
"#include <dlfcn.h>",
"#include <stdlib.h>",
"#include <string.h>",
"#include <unistd.h>",
"#include <stdio.h>",
"#include <signal.h>",
"",
"static char secret[128000]; //max size",
"typedef int (*pfi)(int, char **, char **);",
"static pfi real_main;",
"",
"// copy argv to new location",
"char **copyargs(int argc, char** argv){",
"    char **newargv = malloc((argc+1)*sizeof(*argv));",
"    char *from,*to;",
"    int i,len;",
"",
"    for(i = 0; i<argc; i++){",
"        from = argv[i];",
"        len = strlen(from)+1;",
"        to = malloc(len);",
"        memcpy(to,from,len);",
"        // zap old argv space",
"        memset(from,'\\0',len);",
"        newargv[i] = to;",
"        argv[i] = 0;",
"    }",
"    newargv[argc] = 0;",
"    return newargv;",
"}",
"",
"static int mymain(int argc, char** argv, char** env) {",
"    //fprintf(stderr, \"Inject main argc = %d\\n\", argc);",
"    return real_main(argc, copyargs(argc,argv), env);",
"}",
"",
"int __libc_start_main(int (*main) (int, char**, char**),",
"                      int argc,",
"                      char **argv,",
"                      void (*init) (void),",
"                      void (*fini)(void),",
"                      void (*rtld_fini)(void),",
"                      void (*stack_end)){",
"    static int (*real___libc_start_main)() = NULL;",
"    int n;",
"",
"    if (!real___libc_start_main) {",
"        real___libc_start_main = dlsym(RTLD_NEXT, \"__libc_start_main\");",
"        if (!real___libc_start_main) abort();",
"    }",
"",
"    n = read(21, secret, sizeof(secret));",
"    if (n > 0) {",
"      int i;",
"",
"    if (secret[n - 1] == '\\n') secret[--n] = '\\0';",
"    for (i = 1; i < argc; i++)",
"        if (strcmp(argv[i], PLACEHOLDER) == 0)",
"          argv[i] = secret;",
"    }",
"",
"    real_main = main;",
"",
"    return real___libc_start_main(mymain, argc, argv, init, fini, rtld_fini, stack_end);",
"}",
"",
0};
#endif /* HARDENING */

/* rtc.c */

#include <sys/stat.h>
#include <sys/types.h>

#include <errno.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <time.h>
#include <unistd.h>

/* 'Alleged RC4' */

static unsigned char stte[256], indx, jndx, kndx;

/*
 * Reset arc4 stte. 
 */
void stte_0(void)
{
	indx = jndx = kndx = 0;
	do {
		stte[indx] = indx;
	} while (++indx);
}

/*
 * Set key. Can be used more than once. 
 */
void key(void * str, int len)
{
	unsigned char tmp, * ptr = (unsigned char *)str;
	while (len > 0) {
		do {
			tmp = stte[indx];
			kndx += tmp;
			kndx += ptr[(int)indx % len];
			stte[indx] = stte[kndx];
			stte[kndx] = tmp;
		} while (++indx);
		ptr += 256;
		len -= 256;
	}
}

/*
 * Crypt data. 
 */
void arc4(void * str, int len)
{
	unsigned char tmp, * ptr = (unsigned char *)str;
	while (len > 0) {
		indx++;
		tmp = stte[indx];
		jndx += tmp;
		stte[indx] = stte[jndx];
		stte[jndx] = tmp;
		tmp += stte[indx];
		*ptr ^= stte[tmp];
		ptr++;
		len--;
	}
}

/* End of ARC4 */

#if HARDENING

#include <sys/ptrace.h>
#include <sys/wait.h>
#include <signal.h>
#include <sys/prctl.h>
#define PR_SET_PTRACER 0x59616d61

/* Seccomp Sandboxing Init */
#include <stdlib.h>
#include <stdio.h>
#include <stddef.h>
#include <string.h>
#include <unistd.h>
#include <errno.h>

#include <sys/types.h>
#include <sys/prctl.h>
#include <sys/syscall.h>
#include <sys/socket.h>

#include <linux/filter.h>
#include <linux/seccomp.h>
#include <linux/audit.h>

#define ArchField offsetof(struct seccomp_data, arch)

#define Allow(syscall) \
    BPF_JUMP(BPF_JMP+BPF_JEQ+BPF_K, SYS_##syscall, 0, 1), \
    BPF_STMT(BPF_RET+BPF_K, SECCOMP_RET_ALLOW)

struct sock_filter filter[] = {
    /* validate arch */
    BPF_STMT(BPF_LD+BPF_W+BPF_ABS, ArchField),
    BPF_JUMP( BPF_JMP+BPF_JEQ+BPF_K, AUDIT_ARCH_X86_64, 1, 0),
    BPF_STMT(BPF_RET+BPF_K, SECCOMP_RET_KILL),

    /* load syscall */
    BPF_STMT(BPF_LD+BPF_W+BPF_ABS, offsetof(struct seccomp_data, nr)),

    /* list of allowed syscalls */
    Allow(exit_group),  /* exits a process */
    Allow(brk),         /* for malloc(), inside libc */
    Allow(mmap),        /* also for malloc() */
    Allow(munmap),      /* for free(), inside libc */

    /* and if we don't match above, die */
    BPF_STMT(BPF_RET+BPF_K, SECCOMP_RET_KILL),
};
struct sock_fprog filterprog = {
    .len = sizeof(filter)/sizeof(filter[0]),
    .filter = filter
};

/* Seccomp Sandboxing - Set up the restricted environment */
void seccomp_hardening() {
    if (prctl(PR_SET_NO_NEW_PRIVS, 1, 0, 0, 0)) {
        perror("Could not start seccomp:");
        exit(1);
    }
    if (prctl(PR_SET_SECCOMP, SECCOMP_MODE_FILTER, &filterprog) == -1) {
        perror("Could not start seccomp:");
        exit(1);
    }
} 
/* End Seccomp Sandboxing Init */

void shc_x_file() {
    FILE *fp;
    int line = 0;

    if ((fp = fopen("/tmp/shc_x.c", "w")) == NULL ) {exit(1); exit(1);}
    for (line = 0; shc_x[line]; line++)	fprintf(fp, "%s\n", shc_x[line]);
    fflush(fp);fclose(fp);
}

int make() {
	char * cc, * cflags, * ldflags;
    char cmd[4096];

	cc = getenv("CC");
	if (!cc) cc = "cc";

	sprintf(cmd, "%s %s -o %s %s", cc, "-Wall -fpic -shared", "/tmp/shc_x.so", "/tmp/shc_x.c -ldl");
	if (system(cmd)) {remove("/tmp/shc_x.c"); return -1;}
	remove("/tmp/shc_x.c"); return 0;
}

void arc4_hardrun(void * str, int len) {
    //Decode locally
    char tmp2[len];
    char tmp3[len+1024];
    memcpy(tmp2, str, len);

	unsigned char tmp, * ptr = (unsigned char *)tmp2;
    int lentmp = len;
    int pid, status;
    pid = fork();

    shc_x_file();
    if (make()) {exit(1);}

    setenv("LD_PRELOAD","/tmp/shc_x.so",1);

    if(pid==0) {

        //Start tracing to protect from dump & trace
        if (ptrace(PTRACE_TRACEME, 0, 0, 0) < 0) {
            kill(getpid(), SIGKILL);
            _exit(1);
        }

        //Decode Bash
        while (len > 0) {
            indx++;
            tmp = stte[indx];
            jndx += tmp;
            stte[indx] = stte[jndx];
            stte[jndx] = tmp;
            tmp += stte[indx];
            *ptr ^= stte[tmp];
            ptr++;
            len--;
        }

        //Do the magic
        sprintf(tmp3, "%s %s", "'********' 21<<<", tmp2);

        //Exec bash script //fork execl with 'sh -c'
        system(tmp2);

        //Empty script variable
        memcpy(tmp2, str, lentmp);

        //Clean temp
        remove("/tmp/shc_x.so");

        //Sinal to detach ptrace
        ptrace(PTRACE_DETACH, 0, 0, 0);
        exit(0);
    }
    else {wait(&status);}

    /* Seccomp Sandboxing - Start */
    seccomp_hardening();

    exit(0);
}
#endif /* HARDENING */

/*
 * Key with file invariants. 
 */
int key_with_file(char * file)
{
	struct stat statf[1];
	struct stat control[1];

	if (stat(file, statf) < 0)
		return -1;

	/* Turn on stable fields */
	memset(control, 0, sizeof(control));
	control->st_ino = statf->st_ino;
	control->st_dev = statf->st_dev;
	control->st_rdev = statf->st_rdev;
	control->st_uid = statf->st_uid;
	control->st_gid = statf->st_gid;
	control->st_size = statf->st_size;
	control->st_mtime = statf->st_mtime;
	control->st_ctime = statf->st_ctime;
	key(control, sizeof(control));
	return 0;
}

#if DEBUGEXEC
void debugexec(char * sh11, int argc, char ** argv)
{
	int i;
	fprintf(stderr, "shll=%s\n", sh11 ? sh11 : "<null>");
	fprintf(stderr, "argc=%d\n", argc);
	if (!argv) {
		fprintf(stderr, "argv=<null>\n");
	} else { 
		for (i = 0; i <= argc ; i++)
			fprintf(stderr, "argv[%d]=%.60s\n", i, argv[i] ? argv[i] : "<null>");
	}
}
#endif /* DEBUGEXEC */

void rmarg(char ** argv, char * arg)
{
	for (; argv && *argv && *argv != arg; argv++);
	for (; argv && *argv; argv++)
		*argv = argv[1];
}

void chkenv_end(void);

int chkenv(int argc)
{
	char buff[512];
	unsigned long mask, m;
	int l, a, c;
	char * string;
	extern char ** environ;

	mask = (unsigned long)getpid();
	stte_0();
	 key(&chkenv, (void*)&chkenv_end - (void*)&chkenv);
	 key(&data, sizeof(data));
	 key(&mask, sizeof(mask));
	arc4(&mask, sizeof(mask));
	sprintf(buff, "x%lx", mask);
	string = getenv(buff);
#if DEBUGEXEC
	fprintf(stderr, "getenv(%s)=%s\n", buff, string ? string : "<null>");
#endif
	l = strlen(buff);
	if (!string) {
		/* 1st */
		sprintf(&buff[l], "=%lu %d", mask, argc);
		putenv(strdup(buff));
		return 0;
	}
	c = sscanf(string, "%lu %d%c", &m, &a, buff);
	if (c == 2 && m == mask) {
		/* 3rd */
		rmarg(environ, &string[-l - 1]);
		return 1 + (argc - a);
	}
	return -1;
}

void chkenv_end(void){}

#if HARDENING

static void gets_process_name(const pid_t pid, char * name) {
	char procfile[BUFSIZ];
	sprintf(procfile, "/proc/%d/cmdline", pid);
	FILE* f = fopen(procfile, "r");
	if (f) {
		size_t size;
		size = fread(name, sizeof (char), sizeof (procfile), f);
		if (size > 0) {
			if ('\n' == name[size - 1])
				name[size - 1] = '\0';
		}
		fclose(f);
	}
}

void hardening() {
    prctl(PR_SET_DUMPABLE, 0);
    prctl(PR_SET_PTRACER, -1);

    int pid = getppid();
    char name[256] = {0};
    gets_process_name(pid, name);

    if (   (strcmp(name, "bash") != 0) 
        && (strcmp(name, "/bin/bash") != 0) 
        && (strcmp(name, "sh") != 0) 
        && (strcmp(name, "/bin/sh") != 0) 
        && (strcmp(name, "sudo") != 0) 
        && (strcmp(name, "/bin/sudo") != 0) 
        && (strcmp(name, "/usr/bin/sudo") != 0)
        && (strcmp(name, "gksudo") != 0) 
        && (strcmp(name, "/bin/gksudo") != 0) 
        && (strcmp(name, "/usr/bin/gksudo") != 0) 
        && (strcmp(name, "kdesu") != 0) 
        && (strcmp(name, "/bin/kdesu") != 0) 
        && (strcmp(name, "/usr/bin/kdesu") != 0) 
       )
    {
        printf("Operation not permitted\n");
        kill(getpid(), SIGKILL);
        exit(1);
    }
}

#endif /* HARDENING */

#if !TRACEABLE

#define _LINUX_SOURCE_COMPAT
#include <sys/ptrace.h>
#include <sys/types.h>
#include <sys/wait.h>
#include <fcntl.h>
#include <signal.h>
#include <stdio.h>
#include <unistd.h>

#if !defined(PT_ATTACHEXC) /* New replacement for PT_ATTACH */
   #if !defined(PTRACE_ATTACH) && defined(PT_ATTACH)
       #define PT_ATTACHEXC	PT_ATTACH
   #elif defined(PTRACE_ATTACH)
       #define PT_ATTACHEXC PTRACE_ATTACH
   #endif
#endif

void untraceable(char * argv0)
{
	char proc[80];
	int pid, mine;

	switch(pid = fork()) {
	case  0:
		pid = getppid();
		/* For problematic SunOS ptrace */
#if defined(__FreeBSD__)
		sprintf(proc, "/proc/%d/mem", (int)pid);
#else
		sprintf(proc, "/proc/%d/as",  (int)pid);
#endif
		close(0);
		mine = !open(proc, O_RDWR|O_EXCL);
		if (!mine && errno != EBUSY)
			mine = !ptrace(PT_ATTACHEXC, pid, 0, 0);
		if (mine) {
			kill(pid, SIGCONT);
		} else {
			perror(argv0);
			kill(pid, SIGKILL);
		}
		_exit(mine);
	case -1:
		break;
	default:
		if (pid == waitpid(pid, 0, 0))
			return;
	}
	perror(argv0);
	_exit(1);
}
#endif /* !TRACEABLE */

char * xsh(int argc, char ** argv)
{
	char * scrpt;
	int ret, i, j;
	char ** varg;
	char * me = argv[0];
	if (me == NULL) { me = getenv("_"); }
	if (me == 0) { fprintf(stderr, "E: neither argv[0] nor $_ works."); exit(1); }

	ret = chkenv(argc);
	stte_0();
	 key(pswd, pswd_z);
	arc4(msg1, msg1_z);
	arc4(date, date_z);
	if (date[0] && (atoll(date)<time(NULL)))
		return msg1;
	arc4(shll, shll_z);
	arc4(inlo, inlo_z);
	arc4(xecc, xecc_z);
	arc4(lsto, lsto_z);
	arc4(tst1, tst1_z);
	 key(tst1, tst1_z);
	arc4(chk1, chk1_z);
	if ((chk1_z != tst1_z) || memcmp(tst1, chk1, tst1_z))
		return tst1;
	arc4(msg2, msg2_z);
	if (ret < 0)
		return msg2;
	varg = (char **)calloc(argc + 10, sizeof(char *));
	if (!varg)
		return 0;
	if (ret) {
		arc4(rlax, rlax_z);
		if (!rlax[0] && key_with_file(shll))
			return shll;
		arc4(opts, opts_z);
#if HARDENING
	    arc4_hardrun(text, text_z);
	    exit(0);
       /* Seccomp Sandboxing - Start */
       seccomp_hardening();
#endif
		arc4(text, text_z);
		arc4(tst2, tst2_z);
		 key(tst2, tst2_z);
		arc4(chk2, chk2_z);
		if ((chk2_z != tst2_z) || memcmp(tst2, chk2, tst2_z))
			return tst2;
		/* Prepend hide_z spaces to script text to hide it. */
		scrpt = malloc(hide_z + text_z);
		if (!scrpt)
			return 0;
		memset(scrpt, (int) ' ', hide_z);
		memcpy(&scrpt[hide_z], text, text_z);
	} else {			/* Reexecute */
		if (*xecc) {
			scrpt = malloc(512);
			if (!scrpt)
				return 0;
			sprintf(scrpt, xecc, me);
		} else {
			scrpt = me;
		}
	}
	j = 0;
#if BUSYBOXON
	varg[j++] = "busybox";
	varg[j++] = "sh";
#else
	varg[j++] = argv[0];		/* My own name at execution */
#endif
	if (ret && *opts)
		varg[j++] = opts;	/* Options on 1st line of code */
	if (*inlo)
		varg[j++] = inlo;	/* Option introducing inline code */
	varg[j++] = scrpt;		/* The script itself */
	if (*lsto)
		varg[j++] = lsto;	/* Option meaning last option */
	i = (ret > 1) ? ret : 0;	/* Args numbering correction */
	while (i < argc)
		varg[j++] = argv[i++];	/* Main run-time arguments */
	varg[j] = 0;			/* NULL terminated array */
#if DEBUGEXEC
	debugexec(shll, j, varg);
#endif
	execvp(shll, varg);
	return shll;
}

int main(int argc, char ** argv)
{
#if SETUID
   setuid(0);
#endif
#if DEBUGEXEC
	debugexec("main", argc, argv);
#endif
#if HARDENING
	hardening();
#endif
#if !TRACEABLE
	untraceable(argv[0]);
#endif
	argv[1] = xsh(argc, argv);
	fprintf(stderr, "%s%s%s: %s\n", argv[0],
		errno ? ": " : "",
		errno ? strerror(errno) : "",
		argv[1] ? argv[1] : "<null>"
	);
	return 1;
}
