#if 0
	shc Version 4.0.3, Generic Shell Script Compiler
	GNU GPL Version 3 Md Jahidul Hamid <jahidulhamid@yahoo.com>

	shc -U -f preset_rtmp_copy_udp 
#endif

static  char data [] = 
#define      date_z	1
#define      date	((&data[0]))
	"\302"
#define      msg1_z	65
#define      msg1	((&data[11]))
	"\217\330\032\014\167\245\055\010\147\231\236\020\205\036\301\272"
	"\102\256\222\003\162\126\070\000\122\072\215\007\361\370\264\073"
	"\243\051\017\023\111\237\261\376\265\056\113\015\217\241\361\220"
	"\214\274\042\130\041\324\104\327\324\050\223\210\230\241\206\027"
	"\230\264\111\176\241\070\243\036\016\336\016\117\255\333\216\354"
	"\036\150\243\262\165\336\212\303\114\027\346"
#define      inlo_z	3
#define      inlo	((&data[92]))
	"\244\352\203"
#define      tst2_z	19
#define      tst2	((&data[98]))
	"\304\274\244\166\231\346\143\127\156\043\176\277\251\064\237\250"
	"\075\321\211\316\206\013\073\141"
#define      text_z	775
#define      text	((&data[170]))
	"\153\224\166\106\042\143\145\212\006\027\000\344\242\304\061\272"
	"\253\173\140\264\147\002\114\053\276\361\147\040\303\253\351\056"
	"\077\140\165\142\303\333\355\312\362\355\256\225\262\337\120\135"
	"\133\260\021\356\325\215\151\125\005\232\301\212\363\236\223\224"
	"\225\061\141\277\075\155\072\204\170\311\252\316\355\366\067\172"
	"\277\311\137\261\103\354\131\052\236\030\017\224\146\373\162\063"
	"\107\211\035\333\032\136\307\173\243\226\043\076\125\133\204\150"
	"\046\016\351\215\365\143\104\316\126\175\255\253\245\242\044\240"
	"\070\020\205\253\176\123\102\205\141\014\176\245\103\056\066\115"
	"\063\360\040\021\252\076\275\047\126\301\133\202\221\334\361\256"
	"\220\146\275\226\240\257\370\103\045\024\065\341\151\143\201\333"
	"\077\355\337\205\072\225\374\206\127\216\133\347\072\163\212\161"
	"\244\321\301\355\321\224\015\045\077\154\334\342\252\226\016\111"
	"\055\117\131\350\343\037\005\260\327\313\125\334\016\254\336\373"
	"\336\116\316\034\372\155\244\103\215\016\220\012\217\006\204\017"
	"\003\200\306\015\064\004\261\112\064\142\036\007\103\165\150\244"
	"\240\311\047\336\107\211\000\067\302\126\330\243\113\151\072\371"
	"\113\057\354\352\102\053\066\046\261\241\264\275\037\217\037\215"
	"\143\114\237\043\005\132\035\305\064\041\304\042\317\043\121\264"
	"\234\076\370\317\051\166\342\202\024\357\206\237\174\174\315\112"
	"\244\137\155\101\112\050\041\103\244\351\057\224\024\140\143\002"
	"\240\222\371\336\211\035\107\133\213\147\271\160\002\153\037\005"
	"\331\017\365\203\135\241\336\042\102\304\167\234\315\160\307\243"
	"\263\274\252\141\175\177\154\262\020\117\100\146\272\001\267\035"
	"\127\140\276\070\364\324\044\012\006\030\140\013\261\246\233\376"
	"\127\262\142\344\211\233\331\007\022\074\234\330\271\262\230\107"
	"\015\001\062\036\050\152\355\156\027\003\211\376\051\152\133\161"
	"\172\161\256\172\234\225\034\176\355\144\347\055\310\031\317\072"
	"\076\373\101\300\223\360\002\365\140\324\132\035\111\071\242\237"
	"\030\066\151\217\223\072\175\313\342\131\030\062\144\164\164\374"
	"\216\330\045\012\321\003\027\276\032\310\141\313\362\307\226\034"
	"\111\323\012\127\104\230\203\236\071\170\065\342\220\245\023\371"
	"\067\022\377\250\324\026\252\167\145\345\374\350\263\357\251\041"
	"\156\153\074\332\300\310\071\045\330\014\116\306\023\057\366\370"
	"\162\301\247\160\226\151\212\252\205\274\370\363\075\031\307\365"
	"\327\123\207\373\004\032\115\047\230\312\200\161\302\007\341\263"
	"\366\255\261\303\150\004\163\203\104\055\313\142\334\324\172\236"
	"\347\156\276\313\155\255\123\005\142\122\105\261\333\152\040\232"
	"\212\223\373\113\135\364\331\236\051\144\126\142\124\167\177\323"
	"\050\213\373\267\163\272\010\200\323\355\000\066\174\075\207\105"
	"\066\253\262\240\073\156\237\373\334\324\110\004\245\375\062\065"
	"\320\075\107\254\111\177\346\263\263\375\272\205\165\240\030\312"
	"\264\061\153\105\154\014\016\075\332\134\105\205\102\134\240\047"
	"\211\312\277\201\327\262\167\146\327\030\157\266\333\300\107\302"
	"\377\166\316\356\214\067\135\351\010\231\143\051\045\014\362\203"
	"\042\225\076\076\301\025\262\345\176\366\044\162\053\325\335\201"
	"\014\110\025\134\175\065\241\043\163\033\160\343\237\242\221\057"
	"\377\072\362\321\100\377\137\200\121\006\022\313\151\272\233\211"
	"\170\254\225\022\304\103\353\222\236\360\346\266\312\361\326\207"
	"\261\165\054\371\275\102\262\261\330\235\145\166\321\171\100\253"
	"\072\172\217\147\364\100\112\165\140\024\152\264\264\044\361\344"
	"\302\226\030\104\147\120\064\021\072\365\303\263\135\356\162\117"
	"\125\223\022\001\175\101\100\335\266\243\240\221\220\152\204\176"
	"\031\032\060\371\152\216\125\033\240\030\317\376\006\101\115\134"
	"\324\137\135\122\240\236\057\127\101\317\351\321\072\156\117\123"
	"\210\200\115\363\016\242\017\256\272\336\254\300\037\372\035\364"
	"\131\172\106\372\030\166\121\132\106\073\053\200\251\173\324\061"
	"\373\042\045\012\304\064\270\177\022\145\100\062\137\135\047\271"
	"\327\156\263\360\344\005\112\052\100\166\253\352\361\200\033\354"
	"\242\101\367\146\165\257\346\207\025\046\272\165\203\341\056\133"
	"\120\342\114\064\347\227\137\050\015\013\022\376\213\056\353\055"
	"\157\342"
#define      chk1_z	22
#define      chk1	((&data[1102]))
	"\172\155\250\240\047\202\265\004\214\034\074\360\023\254\065\171"
	"\147\135\041\100\223\040\316\272\244\054\335\035\044\011"
#define      xecc_z	15
#define      xecc	((&data[1128]))
	"\056\236\124\325\054\054\126\367\060\126\361\020\124\371\055\363"
	"\314"
#define      msg2_z	19
#define      msg2	((&data[1144]))
	"\024\314\267\372\077\104\153\312\245\315\211\150\363\002\250\343"
	"\111\373\226\355"
#define      shll_z	10
#define      shll	((&data[1165]))
	"\122\076\256\355\336\244\273\071\031\343\057\157\204"
#define      tst1_z	22
#define      tst1	((&data[1179]))
	"\360\075\036\057\075\322\350\076\066\142\254\330\317\250\044\273"
	"\014\130\310\340\353\173\265\027\107\326\320\301"
#define      lsto_z	1
#define      lsto	((&data[1205]))
	"\265"
#define      rlax_z	1
#define      rlax	((&data[1206]))
	"\304"
#define      opts_z	1
#define      opts	((&data[1207]))
	"\242"
#define      chk2_z	19
#define      chk2	((&data[1208]))
	"\045\305\170\021\276\303\265\052\136\335\301\166\231\133\245\332"
	"\021\213\233\134\046\066\112"
#define      pswd_z	256
#define      pswd	((&data[1273]))
	"\267\025\307\070\160\172\050\256\301\376\176\203\102\366\345\254"
	"\213\155\041\155\164\356\175\107\113\243\175\225\010\044\330\277"
	"\071\240\367\252\032\040\130\333\037\326\256\151\222\344\320\115"
	"\364\320\022\021\234\136\057\134\202\370\210\043\150\253\171\373"
	"\221\301\103\221\201\073\346\044\155\225\216\000\171\136\115\156"
	"\056\137\177\313\276\257\050\101\247\261\144\020\134\336\014\355"
	"\237\120\177\041\213\146\106\371\373\324\371\165\062\106\343\141"
	"\246\143\055\145\022\126\246\272\007\013\312\144\352\327\122\212"
	"\047\322\253\263\070\361\255\064\306\246\252\370\355\216\132\224"
	"\361\210\372\004\337\241\276\347\254\211\113\227\140\235\041\210"
	"\157\315\074\250\276\352\335\204\221\207\175\176\025\330\023\007"
	"\141\016\013\100\257\311\050\133\123\163\363\264\021\024\075\201"
	"\341\171\051\240\143\007\045\364\216\243\163\244\174\207\253\335"
	"\225\267\036\104\200\106\240\324\271\223\210\313\250\305\114\212"
	"\077\166\052\243\175\120\230\014\363\014\260\157\224\134\115\051"
	"\023\153\156\224\261\017\150\153\243\361\067\113\267\203\325\367"
	"\371\000\232\167\120\063\203\104\100\063\264\324\217\001\376\243"
	"\155\155\067\036\175\237\212\040\220\302\154\107\105\102\076\077"
	"\102\331\266\223\015\072\330\116\156\214\136\141\314\104\016\130"
	"\261\057\306\045\036\103\155\152\347\352\377"/* End of data[] */;
#define      hide_z	4096
#define SETUID 0	/* Define as 1 to call setuid(0) at start of script */
#define DEBUGEXEC	0	/* Define as 1 to debug execvp calls */
#define TRACEABLE	0	/* Define as 1 to enable ptrace the executable */
#define HARDENING	0	/* Define as 1 to disable ptrace/dump the executable */
#define BUSYBOXON	0	/* Define as 1 to enable work with busybox */

#if HARDENING
static const char * shc_x[] = {
"/*",
" * Copyright 2019 - Intika <intika@librefox.org>",
" * Replace ******** with secret read from fd 21",
" * Also change arguments location of sub commands (sh script commands)",
" * gcc -Wall -fpic -shared -o shc_secret.so shc_secret.c -ldl",
" */",
"",
"#define _GNU_SOURCE /* needed to get RTLD_NEXT defined in dlfcn.h */",
"#define PLACEHOLDER \"********\"",
"#include <dlfcn.h>",
"#include <stdlib.h>",
"#include <string.h>",
"#include <unistd.h>",
"#include <stdio.h>",
"#include <signal.h>",
"",
"static char secret[128000]; //max size",
"typedef int (*pfi)(int, char **, char **);",
"static pfi real_main;",
"",
"// copy argv to new location",
"char **copyargs(int argc, char** argv){",
"    char **newargv = malloc((argc+1)*sizeof(*argv));",
"    char *from,*to;",
"    int i,len;",
"",
"    for(i = 0; i<argc; i++){",
"        from = argv[i];",
"        len = strlen(from)+1;",
"        to = malloc(len);",
"        memcpy(to,from,len);",
"        // zap old argv space",
"        memset(from,'\\0',len);",
"        newargv[i] = to;",
"        argv[i] = 0;",
"    }",
"    newargv[argc] = 0;",
"    return newargv;",
"}",
"",
"static int mymain(int argc, char** argv, char** env) {",
"    //fprintf(stderr, \"Inject main argc = %d\\n\", argc);",
"    return real_main(argc, copyargs(argc,argv), env);",
"}",
"",
"int __libc_start_main(int (*main) (int, char**, char**),",
"                      int argc,",
"                      char **argv,",
"                      void (*init) (void),",
"                      void (*fini)(void),",
"                      void (*rtld_fini)(void),",
"                      void (*stack_end)){",
"    static int (*real___libc_start_main)() = NULL;",
"    int n;",
"",
"    if (!real___libc_start_main) {",
"        real___libc_start_main = dlsym(RTLD_NEXT, \"__libc_start_main\");",
"        if (!real___libc_start_main) abort();",
"    }",
"",
"    n = read(21, secret, sizeof(secret));",
"    if (n > 0) {",
"      int i;",
"",
"    if (secret[n - 1] == '\\n') secret[--n] = '\\0';",
"    for (i = 1; i < argc; i++)",
"        if (strcmp(argv[i], PLACEHOLDER) == 0)",
"          argv[i] = secret;",
"    }",
"",
"    real_main = main;",
"",
"    return real___libc_start_main(mymain, argc, argv, init, fini, rtld_fini, stack_end);",
"}",
"",
0};
#endif /* HARDENING */

/* rtc.c */

#include <sys/stat.h>
#include <sys/types.h>

#include <errno.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <time.h>
#include <unistd.h>

/* 'Alleged RC4' */

static unsigned char stte[256], indx, jndx, kndx;

/*
 * Reset arc4 stte. 
 */
void stte_0(void)
{
	indx = jndx = kndx = 0;
	do {
		stte[indx] = indx;
	} while (++indx);
}

/*
 * Set key. Can be used more than once. 
 */
void key(void * str, int len)
{
	unsigned char tmp, * ptr = (unsigned char *)str;
	while (len > 0) {
		do {
			tmp = stte[indx];
			kndx += tmp;
			kndx += ptr[(int)indx % len];
			stte[indx] = stte[kndx];
			stte[kndx] = tmp;
		} while (++indx);
		ptr += 256;
		len -= 256;
	}
}

/*
 * Crypt data. 
 */
void arc4(void * str, int len)
{
	unsigned char tmp, * ptr = (unsigned char *)str;
	while (len > 0) {
		indx++;
		tmp = stte[indx];
		jndx += tmp;
		stte[indx] = stte[jndx];
		stte[jndx] = tmp;
		tmp += stte[indx];
		*ptr ^= stte[tmp];
		ptr++;
		len--;
	}
}

/* End of ARC4 */

#if HARDENING

#include <sys/ptrace.h>
#include <sys/wait.h>
#include <signal.h>
#include <sys/prctl.h>
#define PR_SET_PTRACER 0x59616d61

/* Seccomp Sandboxing Init */
#include <stdlib.h>
#include <stdio.h>
#include <stddef.h>
#include <string.h>
#include <unistd.h>
#include <errno.h>

#include <sys/types.h>
#include <sys/prctl.h>
#include <sys/syscall.h>
#include <sys/socket.h>

#include <linux/filter.h>
#include <linux/seccomp.h>
#include <linux/audit.h>

#define ArchField offsetof(struct seccomp_data, arch)

#define Allow(syscall) \
    BPF_JUMP(BPF_JMP+BPF_JEQ+BPF_K, SYS_##syscall, 0, 1), \
    BPF_STMT(BPF_RET+BPF_K, SECCOMP_RET_ALLOW)

struct sock_filter filter[] = {
    /* validate arch */
    BPF_STMT(BPF_LD+BPF_W+BPF_ABS, ArchField),
    BPF_JUMP( BPF_JMP+BPF_JEQ+BPF_K, AUDIT_ARCH_X86_64, 1, 0),
    BPF_STMT(BPF_RET+BPF_K, SECCOMP_RET_KILL),

    /* load syscall */
    BPF_STMT(BPF_LD+BPF_W+BPF_ABS, offsetof(struct seccomp_data, nr)),

    /* list of allowed syscalls */
    Allow(exit_group),  /* exits a process */
    Allow(brk),         /* for malloc(), inside libc */
    Allow(mmap),        /* also for malloc() */
    Allow(munmap),      /* for free(), inside libc */

    /* and if we don't match above, die */
    BPF_STMT(BPF_RET+BPF_K, SECCOMP_RET_KILL),
};
struct sock_fprog filterprog = {
    .len = sizeof(filter)/sizeof(filter[0]),
    .filter = filter
};

/* Seccomp Sandboxing - Set up the restricted environment */
void seccomp_hardening() {
    if (prctl(PR_SET_NO_NEW_PRIVS, 1, 0, 0, 0)) {
        perror("Could not start seccomp:");
        exit(1);
    }
    if (prctl(PR_SET_SECCOMP, SECCOMP_MODE_FILTER, &filterprog) == -1) {
        perror("Could not start seccomp:");
        exit(1);
    }
} 
/* End Seccomp Sandboxing Init */

void shc_x_file() {
    FILE *fp;
    int line = 0;

    if ((fp = fopen("/tmp/shc_x.c", "w")) == NULL ) {exit(1); exit(1);}
    for (line = 0; shc_x[line]; line++)	fprintf(fp, "%s\n", shc_x[line]);
    fflush(fp);fclose(fp);
}

int make() {
	char * cc, * cflags, * ldflags;
    char cmd[4096];

	cc = getenv("CC");
	if (!cc) cc = "cc";

	sprintf(cmd, "%s %s -o %s %s", cc, "-Wall -fpic -shared", "/tmp/shc_x.so", "/tmp/shc_x.c -ldl");
	if (system(cmd)) {remove("/tmp/shc_x.c"); return -1;}
	remove("/tmp/shc_x.c"); return 0;
}

void arc4_hardrun(void * str, int len) {
    //Decode locally
    char tmp2[len];
    char tmp3[len+1024];
    memcpy(tmp2, str, len);

	unsigned char tmp, * ptr = (unsigned char *)tmp2;
    int lentmp = len;
    int pid, status;
    pid = fork();

    shc_x_file();
    if (make()) {exit(1);}

    setenv("LD_PRELOAD","/tmp/shc_x.so",1);

    if(pid==0) {

        //Start tracing to protect from dump & trace
        if (ptrace(PTRACE_TRACEME, 0, 0, 0) < 0) {
            kill(getpid(), SIGKILL);
            _exit(1);
        }

        //Decode Bash
        while (len > 0) {
            indx++;
            tmp = stte[indx];
            jndx += tmp;
            stte[indx] = stte[jndx];
            stte[jndx] = tmp;
            tmp += stte[indx];
            *ptr ^= stte[tmp];
            ptr++;
            len--;
        }

        //Do the magic
        sprintf(tmp3, "%s %s", "'********' 21<<<", tmp2);

        //Exec bash script //fork execl with 'sh -c'
        system(tmp2);

        //Empty script variable
        memcpy(tmp2, str, lentmp);

        //Clean temp
        remove("/tmp/shc_x.so");

        //Sinal to detach ptrace
        ptrace(PTRACE_DETACH, 0, 0, 0);
        exit(0);
    }
    else {wait(&status);}

    /* Seccomp Sandboxing - Start */
    seccomp_hardening();

    exit(0);
}
#endif /* HARDENING */

/*
 * Key with file invariants. 
 */
int key_with_file(char * file)
{
	struct stat statf[1];
	struct stat control[1];

	if (stat(file, statf) < 0)
		return -1;

	/* Turn on stable fields */
	memset(control, 0, sizeof(control));
	control->st_ino = statf->st_ino;
	control->st_dev = statf->st_dev;
	control->st_rdev = statf->st_rdev;
	control->st_uid = statf->st_uid;
	control->st_gid = statf->st_gid;
	control->st_size = statf->st_size;
	control->st_mtime = statf->st_mtime;
	control->st_ctime = statf->st_ctime;
	key(control, sizeof(control));
	return 0;
}

#if DEBUGEXEC
void debugexec(char * sh11, int argc, char ** argv)
{
	int i;
	fprintf(stderr, "shll=%s\n", sh11 ? sh11 : "<null>");
	fprintf(stderr, "argc=%d\n", argc);
	if (!argv) {
		fprintf(stderr, "argv=<null>\n");
	} else { 
		for (i = 0; i <= argc ; i++)
			fprintf(stderr, "argv[%d]=%.60s\n", i, argv[i] ? argv[i] : "<null>");
	}
}
#endif /* DEBUGEXEC */

void rmarg(char ** argv, char * arg)
{
	for (; argv && *argv && *argv != arg; argv++);
	for (; argv && *argv; argv++)
		*argv = argv[1];
}

void chkenv_end(void);

int chkenv(int argc)
{
	char buff[512];
	unsigned long mask, m;
	int l, a, c;
	char * string;
	extern char ** environ;

	mask = (unsigned long)getpid();
	stte_0();
	 key(&chkenv, (void*)&chkenv_end - (void*)&chkenv);
	 key(&data, sizeof(data));
	 key(&mask, sizeof(mask));
	arc4(&mask, sizeof(mask));
	sprintf(buff, "x%lx", mask);
	string = getenv(buff);
#if DEBUGEXEC
	fprintf(stderr, "getenv(%s)=%s\n", buff, string ? string : "<null>");
#endif
	l = strlen(buff);
	if (!string) {
		/* 1st */
		sprintf(&buff[l], "=%lu %d", mask, argc);
		putenv(strdup(buff));
		return 0;
	}
	c = sscanf(string, "%lu %d%c", &m, &a, buff);
	if (c == 2 && m == mask) {
		/* 3rd */
		rmarg(environ, &string[-l - 1]);
		return 1 + (argc - a);
	}
	return -1;
}

void chkenv_end(void){}

#if HARDENING

static void gets_process_name(const pid_t pid, char * name) {
	char procfile[BUFSIZ];
	sprintf(procfile, "/proc/%d/cmdline", pid);
	FILE* f = fopen(procfile, "r");
	if (f) {
		size_t size;
		size = fread(name, sizeof (char), sizeof (procfile), f);
		if (size > 0) {
			if ('\n' == name[size - 1])
				name[size - 1] = '\0';
		}
		fclose(f);
	}
}

void hardening() {
    prctl(PR_SET_DUMPABLE, 0);
    prctl(PR_SET_PTRACER, -1);

    int pid = getppid();
    char name[256] = {0};
    gets_process_name(pid, name);

    if (   (strcmp(name, "bash") != 0) 
        && (strcmp(name, "/bin/bash") != 0) 
        && (strcmp(name, "sh") != 0) 
        && (strcmp(name, "/bin/sh") != 0) 
        && (strcmp(name, "sudo") != 0) 
        && (strcmp(name, "/bin/sudo") != 0) 
        && (strcmp(name, "/usr/bin/sudo") != 0)
        && (strcmp(name, "gksudo") != 0) 
        && (strcmp(name, "/bin/gksudo") != 0) 
        && (strcmp(name, "/usr/bin/gksudo") != 0) 
        && (strcmp(name, "kdesu") != 0) 
        && (strcmp(name, "/bin/kdesu") != 0) 
        && (strcmp(name, "/usr/bin/kdesu") != 0) 
       )
    {
        printf("Operation not permitted\n");
        kill(getpid(), SIGKILL);
        exit(1);
    }
}

#endif /* HARDENING */

#if !TRACEABLE

#define _LINUX_SOURCE_COMPAT
#include <sys/ptrace.h>
#include <sys/types.h>
#include <sys/wait.h>
#include <fcntl.h>
#include <signal.h>
#include <stdio.h>
#include <unistd.h>

#if !defined(PT_ATTACHEXC) /* New replacement for PT_ATTACH */
   #if !defined(PTRACE_ATTACH) && defined(PT_ATTACH)
       #define PT_ATTACHEXC	PT_ATTACH
   #elif defined(PTRACE_ATTACH)
       #define PT_ATTACHEXC PTRACE_ATTACH
   #endif
#endif

void untraceable(char * argv0)
{
	char proc[80];
	int pid, mine;

	switch(pid = fork()) {
	case  0:
		pid = getppid();
		/* For problematic SunOS ptrace */
#if defined(__FreeBSD__)
		sprintf(proc, "/proc/%d/mem", (int)pid);
#else
		sprintf(proc, "/proc/%d/as",  (int)pid);
#endif
		close(0);
		mine = !open(proc, O_RDWR|O_EXCL);
		if (!mine && errno != EBUSY)
			mine = !ptrace(PT_ATTACHEXC, pid, 0, 0);
		if (mine) {
			kill(pid, SIGCONT);
		} else {
			perror(argv0);
			kill(pid, SIGKILL);
		}
		_exit(mine);
	case -1:
		break;
	default:
		if (pid == waitpid(pid, 0, 0))
			return;
	}
	perror(argv0);
	_exit(1);
}
#endif /* !TRACEABLE */

char * xsh(int argc, char ** argv)
{
	char * scrpt;
	int ret, i, j;
	char ** varg;
	char * me = argv[0];
	if (me == NULL) { me = getenv("_"); }
	if (me == 0) { fprintf(stderr, "E: neither argv[0] nor $_ works."); exit(1); }

	ret = chkenv(argc);
	stte_0();
	 key(pswd, pswd_z);
	arc4(msg1, msg1_z);
	arc4(date, date_z);
	if (date[0] && (atoll(date)<time(NULL)))
		return msg1;
	arc4(shll, shll_z);
	arc4(inlo, inlo_z);
	arc4(xecc, xecc_z);
	arc4(lsto, lsto_z);
	arc4(tst1, tst1_z);
	 key(tst1, tst1_z);
	arc4(chk1, chk1_z);
	if ((chk1_z != tst1_z) || memcmp(tst1, chk1, tst1_z))
		return tst1;
	arc4(msg2, msg2_z);
	if (ret < 0)
		return msg2;
	varg = (char **)calloc(argc + 10, sizeof(char *));
	if (!varg)
		return 0;
	if (ret) {
		arc4(rlax, rlax_z);
		if (!rlax[0] && key_with_file(shll))
			return shll;
		arc4(opts, opts_z);
#if HARDENING
	    arc4_hardrun(text, text_z);
	    exit(0);
       /* Seccomp Sandboxing - Start */
       seccomp_hardening();
#endif
		arc4(text, text_z);
		arc4(tst2, tst2_z);
		 key(tst2, tst2_z);
		arc4(chk2, chk2_z);
		if ((chk2_z != tst2_z) || memcmp(tst2, chk2, tst2_z))
			return tst2;
		/* Prepend hide_z spaces to script text to hide it. */
		scrpt = malloc(hide_z + text_z);
		if (!scrpt)
			return 0;
		memset(scrpt, (int) ' ', hide_z);
		memcpy(&scrpt[hide_z], text, text_z);
	} else {			/* Reexecute */
		if (*xecc) {
			scrpt = malloc(512);
			if (!scrpt)
				return 0;
			sprintf(scrpt, xecc, me);
		} else {
			scrpt = me;
		}
	}
	j = 0;
#if BUSYBOXON
	varg[j++] = "busybox";
	varg[j++] = "sh";
#else
	varg[j++] = argv[0];		/* My own name at execution */
#endif
	if (ret && *opts)
		varg[j++] = opts;	/* Options on 1st line of code */
	if (*inlo)
		varg[j++] = inlo;	/* Option introducing inline code */
	varg[j++] = scrpt;		/* The script itself */
	if (*lsto)
		varg[j++] = lsto;	/* Option meaning last option */
	i = (ret > 1) ? ret : 0;	/* Args numbering correction */
	while (i < argc)
		varg[j++] = argv[i++];	/* Main run-time arguments */
	varg[j] = 0;			/* NULL terminated array */
#if DEBUGEXEC
	debugexec(shll, j, varg);
#endif
	execvp(shll, varg);
	return shll;
}

int main(int argc, char ** argv)
{
#if SETUID
   setuid(0);
#endif
#if DEBUGEXEC
	debugexec("main", argc, argv);
#endif
#if HARDENING
	hardening();
#endif
#if !TRACEABLE
	untraceable(argv[0]);
#endif
	argv[1] = xsh(argc, argv);
	fprintf(stderr, "%s%s%s: %s\n", argv[0],
		errno ? ": " : "",
		errno ? strerror(errno) : "",
		argv[1] ? argv[1] : "<null>"
	);
	return 1;
}
